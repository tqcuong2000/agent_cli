# agent-cli

A basic python module project template.

## Setup

1.  Clone the repository.
2.  Install in editable mode:
    ```bash
    pip install -e .
    ```

## Usage

Run the module directly:
```bash
python -m agent_cli
```

Or use the installed script:
```bash
agent-cli
```
