"""
Tool System — the agent's interface to the host operating system.

Tools bridge the gap between the Agent's reasoning (text generation)
and the host OS.  They are the only mechanism through which an Agent
can read files, search code, or execute commands.
"""
