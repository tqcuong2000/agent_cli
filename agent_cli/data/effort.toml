[LOW]
max_iterations = 200
model_tier = "fast"
reasoning_instruction = "Act immediately. Use tools directly when the path is clear. Keep reasoning brief."
review_policy = "none"

[MEDIUM]
max_iterations = 200
model_tier = "capable"
reasoning_instruction = "Think step-by-step. Use tools to investigate before acting. Verify results before giving a final answer."
review_policy = "standard"

[HIGH]
max_iterations = 400
model_tier = "premium"
reasoning_instruction = "Think deeply. Consider multiple approaches before choosing one. Use tools to verify assumptions. After completing the task, review your work for correctness."
review_policy = "self_verify"

[XHIGH]
max_iterations = 1000
model_tier = "premium"
reasoning_instruction = "Think exhaustively. Leave no stone unturned. Methodically plan every step, verify all assumptions with tools, and rigorously double-check the final result."
review_policy = "strict_self_verify"
